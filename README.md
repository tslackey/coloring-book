# coloring-book

Desktop app that turns images into coloring pages. Open a file, paste from the clipboard, or search Bulbapedia for Pokémon art, then scrub the edge threshold and export a PNG. Originals and outputs are stored locally in SQLite.

```bash
npm install
npm run tauri dev
```
